import { Outfit } from 'next/font/google'


const LogoFont = Outfit({
      weight  : ['600'],
      subsets : ['latin']
});

export default function signup() {
    return (
        <div>
            <p>hello</p>
        </div>
    )
}